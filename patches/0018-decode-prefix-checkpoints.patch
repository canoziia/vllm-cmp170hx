diff --git a/vllm/v1/core/sched/async_scheduler.py b/vllm/v1/core/sched/async_scheduler.py
index 56a532b..cd69d50 100644
--- a/vllm/v1/core/sched/async_scheduler.py
+++ b/vllm/v1/core/sched/async_scheduler.py
@@ -64,7 +64,18 @@ class AsyncScheduler(Scheduler):
 
         # Cache the new tokens. Preempted requests should be skipped.
         if status_before_update == RequestStatus.RUNNING:
-            self.kv_cache_manager.cache_blocks(
-                request, request.num_computed_tokens - request.num_output_placeholders
+            request._cache_confirmed_decode = (
+                self.mamba_partial_cache_hit
+                and self.use_v2_model_runner
+                and not stopped  # EOS/length may trim sampled tokens after GPU state advanced.
+                and self.parallel_config.pipeline_parallel_size == 1
+                and request.num_in_flight_tokens == 0
+                and request.num_computed_tokens > request.num_prompt_tokens
             )
+            try:
+                self.kv_cache_manager.cache_blocks(
+                    request, request.num_computed_tokens - request.num_output_placeholders
+                )
+            finally:
+                request._cache_confirmed_decode = False
         return new_token_ids, stopped
diff --git a/vllm/v1/core/sched/scheduler.py b/vllm/v1/core/sched/scheduler.py
index 8dad2ed..095ef9c 100644
--- a/vllm/v1/core/sched/scheduler.py
+++ b/vllm/v1/core/sched/scheduler.py
@@ -387,6 +387,21 @@ class Scheduler(SchedulerInterface):
         # resumed requests replaying their output tokens.
         prefill_end = max(request.num_prompt_tokens, request.num_tokens - 1)
         if start >= prefill_end:
+            if (
+                self.mamba_partial_cache_hit
+                and self.use_v2_model_runner
+                and self.scheduler_config.async_scheduling
+                and self.parallel_config.pipeline_parallel_size == 1
+            ):
+                # Materialize accepted decode states at fine hash boundaries.
+                # Never schedule past a boundary using unresolved placeholders:
+                # the prior acceptance must be processed before CoW admission.
+                unit = self.hash_block_size
+                if request.num_in_flight_tokens and (
+                    start % unit == 0 or start % unit + num_new_tokens >= unit
+                ):
+                    return 0
+                return min(num_new_tokens, unit - start % unit)
             return num_new_tokens
 
         block_size = self.block_size
diff --git a/vllm/v1/core/single_type_kv_cache_manager.py b/vllm/v1/core/single_type_kv_cache_manager.py
index 09772b9..2aff902 100644
--- a/vllm/v1/core/single_type_kv_cache_manager.py
+++ b/vllm/v1/core/single_type_kv_cache_manager.py
@@ -806,6 +806,10 @@ class FullAttentionManager(SingleTypeKVCacheManager):
         """
         hash_block_size = self.block_pool.hash_block_size
         boundary_tokens = request.num_prompt_tokens // hash_block_size * hash_block_size
+        if getattr(request, '_cache_confirmed_decode', False):
+            # Append-only attention KV at a finalized decode hash boundary.
+            # Future MTP lookup drops one unit; never invent a future marker.
+            boundary_tokens = num_tokens // hash_block_size * hash_block_size
         if boundary_tokens == 0:
             return
         if boundary_tokens > num_tokens:
@@ -1390,6 +1394,11 @@ class MambaManager(SingleTypeKVCacheManager):
             # into a private cow_block; we record that block for connector
             # offload (see _pending_partial_tail_offloads).
             self._producer_partial_tail_reqs: dict[str, int] = {}
+            # Keep only the latest three decode snapshots per producer. MTP
+            # lookup drops one hash unit, hence retaining just one is unsafe.
+            # Prompt snapshots are independent and are never retired here.
+            self._decode_tail_boundaries: dict[str, int] = {}
+            self._decode_tail_snapshots: dict[str, list[tuple[KVCacheBlock, BlockHashWithGroupId]]] = {}
 
     @classmethod
     def find_longest_cache_hit(
@@ -1753,6 +1762,15 @@ class MambaManager(SingleTypeKVCacheManager):
                                     boundary_tokens,
                                 )
                             )
+                            if self._decode_tail_boundaries.pop(request_id, None) == boundary_tokens:
+                                snapshots = self._decode_tail_snapshots.setdefault(request_id, [])
+                                assert cow_block.block_hash is not None
+                                snapshots.append((cow_block, cow_block.block_hash))
+                                while len(snapshots) > 3:
+                                    retired, expected_hash = snapshots.pop(0)
+                                    # Objects in the free pool may have been recycled.
+                                    if retired.block_hash == expected_hash:
+                                        self.block_pool.evict_blocks({retired.block_id})
                         if cow_block.block_hash is not None:
                             # The moved entry is only filled by this step's
                             # copy, so defer same-step hits on it.
@@ -1771,6 +1789,8 @@ class MambaManager(SingleTypeKVCacheManager):
             self._allocated_block_reqs.discard(request_id)
             self.last_state_block_idx.pop(request_id, None)
             self._producer_partial_tail_reqs.pop(request_id, None)
+            self._decode_tail_boundaries.pop(request_id, None)
+            self._decode_tail_snapshots.pop(request_id, None)
             # A hand-off whose request died in this same scheduling pass must
             # not reach the connector: its unpin hook (free) has already run.
             self._pending_partial_tail_offloads = [
@@ -1839,6 +1859,9 @@ class MambaManager(SingleTypeKVCacheManager):
             if self.use_eagle
             else latest_prompt_hash_boundary
         )
+        if getattr(request, '_cache_confirmed_decode', False):
+            cacheable_prompt_boundary = num_tokens
+            self._decode_tail_boundaries[request.request_id] = num_tokens
         if num_tokens != cacheable_prompt_boundary:
             return None
 
diff --git a/vllm/v1/worker/gpu/model_states/mamba_hybrid.py b/vllm/v1/worker/gpu/model_states/mamba_hybrid.py
index 4bc8c6b..28c02d5 100644
--- a/vllm/v1/worker/gpu/model_states/mamba_hybrid.py
+++ b/vllm/v1/worker/gpu/model_states/mamba_hybrid.py
@@ -343,6 +343,15 @@ class MambaHybridModelState(DefaultModelState):
                 self._mamba_state_idx_gpu,
                 num_computed_tokens,
                 idx_mapping,
+                decode_checkpoint_unit=(
+                    (self.cache_config.prefix_match_unit or 0)
+                    if (
+                        self.cache_config.enable_prefix_caching
+                        and self.vllm_config.scheduler_config.async_scheduling
+                        and self.vllm_config.parallel_config.pipeline_parallel_size == 1
+                    )
+                    else 0
+                ),
             )
 
 
diff --git a/vllm/v1/worker/mamba_utils.py b/vllm/v1/worker/mamba_utils.py
index 81dcf23..f320a57 100644
--- a/vllm/v1/worker/mamba_utils.py
+++ b/vllm/v1/worker/mamba_utils.py
@@ -341,6 +341,7 @@ def postprocess_mamba_fused_kernel(
     # PRECOMPUTED_NEW_COMPUTED: when True, num_computed_tokens_ptr already holds
     # the post-step new_num_computed value (V2 supplies the advanced count).
     PRECOMPUTED_NEW_COMPUTED: tl.constexpr = False,
+    DECODE_CHECKPOINT_UNIT: tl.constexpr = 0,
     # TEMPORAL_TILES: when > 1, the temporal copy body is partitioned across
     # TEMPORAL_TILES CTAs along the u64 inner range. Callers must launch a
     # 3D grid (num_reqs, total_states, TEMPORAL_TILES). Default 1 preserves
@@ -391,13 +392,23 @@ def postprocess_mamba_fused_kernel(
     aligned_new_computed = (new_num_computed // block_size) * block_size
 
     needs_copy = aligned_new_computed >= num_tokens_running_state
-
-    if not needs_copy:
+    fine_checkpoint = False
+    if DECODE_CHECKPOINT_UNIT > 0:
+        fine_checkpoint = (
+            new_num_computed > 0
+            and new_num_computed % DECODE_CHECKPOINT_UNIT == 0
+            and new_num_computed % block_size != 0
+        )
+    if not needs_copy and not fine_checkpoint:
         return
 
-    # Compute copy parameters
+    # On a fine boundary normalize the accepted recurrent state into the
+    # running slot. Scheduler CoW will preserve that slot before next forward.
     accept_token_bias = aligned_new_computed - num_tokens_running_state
     dest_block_idx = aligned_new_computed // block_size - 1
+    if fine_checkpoint:
+        accept_token_bias = num_accepted - 1
+        dest_block_idx = src_block_idx
 
     # Update accepted-token count before early exits (per-request, so only
     # state_idx == 0 writes). Also guard on tile_idx == 0 so tiles > 0
@@ -1117,6 +1128,7 @@ class MambaSpecDecodeGPUContext:
         state_idx_gpu: torch.Tensor,
         new_num_computed_tokens_gpu: torch.Tensor,
         idx_mapping: torch.Tensor,
+        decode_checkpoint_unit: int = 0,
     ) -> None:
         """V2 align postprocess: save the running state to the block-aligned
         position after spec-decode acceptance leaves the sequence non-aligned.
@@ -1162,6 +1174,7 @@ class MambaSpecDecodeGPUContext:
             CONV_STATE_DIM_FIRST=is_conv_state_dim_first(),
             HAS_IDX_MAPPING=True,
             PRECOMPUTED_NEW_COMPUTED=True,
+            DECODE_CHECKPOINT_UNIT=decode_checkpoint_unit,
             TEMPORAL_TILES=_TEMPORAL_TILES,
         )
 
