diff --git a/vllm/v1/core/sched/scheduler.py b/vllm/v1/core/sched/scheduler.py
index 77f7d291a..3b8d8cb29 100644
--- a/vllm/v1/core/sched/scheduler.py
+++ b/vllm/v1/core/sched/scheduler.py
@@ -1,6 +1,7 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 import itertools
+import os
 import time
 from collections import defaultdict, deque
 from collections.abc import Iterable
@@ -270,6 +271,15 @@ class Scheduler(SchedulerInterface):
         # manager's re-prefillable window (this minus 1), and how many tokens to
         # reserve between a chunk boundary and the prefill end.
         self.num_prefill_lookahead = 0
+        self._dspark_control = os.environ.get("VLLM_DSPARK_CONTROL_FILE", "")
+        self._dspark_poll_at = 0.0
+        self._dspark_enabled = True
+        if self._dspark_control:
+            if not (speculative_config and speculative_config.use_dspark()
+                    and vllm_config.use_v2_model_runner
+                    and self.parallel_config.data_parallel_size == 1
+                    and not speculative_config.enable_adaptive_verification):
+                raise ValueError("Hot DSpark requires MRV2 DSpark, DP=1, no adaptive verification")
         self.dynamic_sd_lookup: list[int] | None = None
         if speculative_config is not None:
             if speculative_config.num_speculative_tokens_per_batch_size:
@@ -1037,7 +1047,8 @@ class Scheduler(SchedulerInterface):
                     # preserve full cudagraph for this step.
                     # Not for diffusion where draft tokens can't be padded.
                     if (
-                        (self.num_spec_tokens > 0 and self.dynamic_sd_lookup is None)
+                        (self.num_spec_tokens > 0 and self.dynamic_sd_lookup is None
+                         and not self._dspark_control)
                         and self.num_sampled_tokens_per_step > 0
                         and num_new_tokens == 1
                         and not prefill_scheduled
@@ -1393,6 +1404,25 @@ class Scheduler(SchedulerInterface):
                 len(num_scheduled_tokens)
             ]
 
+        if self._dspark_control:
+            now = time.monotonic()
+            if now >= self._dspark_poll_at:
+                self._dspark_poll_at = now + 1.0
+                try:
+                    with open(self._dspark_control) as f:
+                        value = f.read(16).strip()
+                    if value in ("0", "1"):
+                        enabled = value == "1"
+                        if enabled != self._dspark_enabled:
+                            logger.info("Hot DSpark: requested K=%d",
+                                        self.num_spec_tokens if enabled else 0)
+                        self._dspark_enabled = enabled
+                except OSError:
+                    pass
+            num_spec_tokens_to_schedule = (
+                self.num_spec_tokens if self._dspark_enabled else 0
+            )
+
         scheduled_encoder_input_stats = None
         if (
             self.log_stats
diff --git a/vllm/v1/worker/gpu/cudagraph_utils.py b/vllm/v1/worker/gpu/cudagraph_utils.py
index 274cf1f54..2c0f99f5e 100644
--- a/vllm/v1/worker/gpu/cudagraph_utils.py
+++ b/vllm/v1/worker/gpu/cudagraph_utils.py
@@ -2,6 +2,7 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 import gc
 import itertools
+import os
 from collections import defaultdict
 from collections.abc import Callable, Iterable
 from dataclasses import dataclass
@@ -258,6 +259,11 @@ class CudaGraphManager:
         else:
             decode_query_lens = [self.decode_query_len]
 
+        if (isinstance(self, ModelCudaGraphManager)
+                and os.environ.get("VLLM_DSPARK_CONTROL_FILE")
+                and speculative_config and speculative_config.use_dspark()):
+            decode_query_lens = sorted(set(decode_query_lens) | {1, self.decode_query_len})
+
         capture_varlen_decode = (
             separate_decode_routine and bool(decode_mode) and self.varlen_decode
         )
diff --git a/vllm/v1/worker/gpu/model_runner.py b/vllm/v1/worker/gpu/model_runner.py
index 05bc6b8f5..e85ecb6cb 100644
--- a/vllm/v1/worker/gpu/model_runner.py
+++ b/vllm/v1/worker/gpu/model_runner.py
@@ -19,6 +19,7 @@ instead of embedding feature-specific logic directly.
 
 import functools
 import gc
+import os
 import time
 from contextlib import AbstractContextManager
 from copy import deepcopy
@@ -272,6 +273,7 @@ class GPUModelRunner(LoRAModelRunnerMixin):
         self.speculator = None
         self.use_aux_hidden_state_outputs = False
         self.num_speculative_steps = vllm_config.num_speculative_tokens
+        self._hot_dspark = bool(os.environ.get("VLLM_DSPARK_CONTROL_FILE"))
         if self.speculative_config is not None:
             if self.is_last_pp_rank:
                 self.speculator = init_speculator(self.vllm_config, self.device)
@@ -1917,6 +1919,8 @@ class GPUModelRunner(LoRAModelRunnerMixin):
             ec_connector_output=ec_connector_output,
             routed_experts=routed_experts,
             cudagraph_stats=cudagraph_stats,
+            next_draft_k=(scheduler_output.num_spec_tokens_to_schedule
+                          if self._hot_dspark else self.num_speculative_steps),
         )
 
         if not self.is_last_pp_rank:
@@ -1947,6 +1951,7 @@ class GPUModelRunner(LoRAModelRunnerMixin):
         ec_connector_output = self.execute_model_state.ec_connector_output
         routed_experts = self.execute_model_state.routed_experts
         cudagraph_stats = self.execute_model_state.cudagraph_stats
+        next_draft_k = self.execute_model_state.next_draft_k
         self.execute_model_state = None
 
         if not self.is_last_pp_rank:
@@ -2074,9 +2079,12 @@ class GPUModelRunner(LoRAModelRunnerMixin):
                     self.sampler.sampling_states.seeds.gpu,
                     dp_sync=dp_sync,
                     mm_inputs=mm_inputs,
+                    **({"context_only": next_draft_k == 0}
+                       if self._hot_dspark else {}),
                 )
-            self.req_states.draft_tokens[input_batch.idx_mapping] = draft_tokens
-            if self.adaptive_verification is not None:
+            if next_draft_k:
+                self.req_states.draft_tokens[input_batch.idx_mapping] = draft_tokens
+            if self.adaptive_verification is not None and next_draft_k:
                 self.adaptive_verification.record_confidences(
                     self.speculator.draft_token_confidence_probs, input_batch
                 )
@@ -2086,7 +2094,7 @@ class GPUModelRunner(LoRAModelRunnerMixin):
             # not have a speculator (i.e. self.speculator is None)
             self.draft_tokens_handler.set_draft_tokens(
                 input_batch,
-                self.req_states.draft_tokens[input_batch.idx_mapping],
+                self.req_states.draft_tokens[input_batch.idx_mapping, :next_draft_k],
             )
             if self.pp_handler is not None:
                 self.pp_handler.broadcast_drafts(
@@ -2232,6 +2240,7 @@ class ExecuteModelState(NamedTuple):
     ec_connector_output: ECConnectorOutput | None
     routed_experts: RoutedExpertsTensors | None
     cudagraph_stats: CUDAGraphStat | None
+    next_draft_k: int
 
 
 class BatchReqState(NamedTuple):
diff --git a/vllm/v1/worker/gpu/spec_decode/dflash/speculator.py b/vllm/v1/worker/gpu/spec_decode/dflash/speculator.py
index a3e8df792..7f5d6d526 100644
--- a/vllm/v1/worker/gpu/spec_decode/dflash/speculator.py
+++ b/vllm/v1/worker/gpu/spec_decode/dflash/speculator.py
@@ -346,6 +346,7 @@ class DFlashSpeculator(DraftModelSpeculator):
         skip_attn_for_dummy_run: bool = False,
         mm_inputs: tuple[list[torch.Tensor], torch.Tensor] | None = None,
         is_profile: bool = False,
+        context_only: bool = False,
     ) -> torch.Tensor:
         num_reqs = input_batch.num_reqs
         num_target_tokens = input_batch.num_tokens
@@ -443,6 +444,9 @@ class DFlashSpeculator(DraftModelSpeculator):
             context_slots,
         )
 
+        if context_only:
+            return self.draft_tokens[:num_reqs, :0]
+
         batch_sync, num_batch_tokens = (
             self._build_uniform_batch_dp_sync(dp_sync, num_reqs, self.num_query_per_req)
             if dp_sync is not None
