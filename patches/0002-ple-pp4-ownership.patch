diff --git a/vllm/models/qwen3_8_flash_next/nvidia/model_state.py b/vllm/models/qwen3_8_flash_next/nvidia/model_state.py
index d99f51a..3c06b10 100644
--- a/vllm/models/qwen3_8_flash_next/nvidia/model_state.py
+++ b/vllm/models/qwen3_8_flash_next/nvidia/model_state.py
@@ -8,6 +8,8 @@ import torch
 import torch.nn as nn
 
 from vllm.config import VllmConfig
+from vllm.distributed.parallel_state import get_pp_group
+from vllm.distributed.utils import get_pp_indices
 from vllm.v1.worker.gpu.input_batch import InputBatch
 from vllm.v1.worker.gpu.mm.encoder_cache import EncoderCache
 from vllm.v1.worker.gpu.model_states.mamba_hybrid import MambaHybridModelState
@@ -26,20 +28,21 @@ class Qwen3_8FlashNextModelState(MambaHybridModelState):
     ) -> None:
         super().__init__(vllm_config, model, encoder_cache, device)
         config = self.model_config.hf_text_config
-        self.uses_ngram_embedding = bool(config.ple_layer_ids)
+        ple_layers = tuple(int(layer_id) - 1 for layer_id in config.ple_layer_ids)
+        pp_size = vllm_config.parallel_config.pipeline_parallel_size
+        first_start, first_end = get_pp_indices(config.num_hidden_layers, 0, pp_size)
+        if ple_layers and not all(first_start <= layer < first_end for layer in ple_layers):
+            raise RuntimeError(
+                "All Qwen3.8 PLE layers must be assigned to the first pipeline "
+                f"stage so they receive raw input_ids; PLE layers={ple_layers}, "
+                f"first-stage range=[{first_start}, {first_end})."
+            )
+        self.uses_ngram_embedding = bool(ple_layers) and get_pp_group().is_first_rank
         if not self.uses_ngram_embedding:
             self.ngram_context_len = 0
             self.ngram_eos_token_id = 0
             return
 
-        if vllm_config.parallel_config.pipeline_parallel_size > 1:
-            raise RuntimeError(
-                "N-gram PLE embedding currently requires "
-                "pipeline_parallel_size=1 because non-first pipeline ranks do "
-                "not receive the raw input_ids required by PLE. Please run "
-                "with PP=1."
-            )
-
         self.ngram_context_len = int(config.ngram_size) - 1
         if self.ngram_context_len <= 0:
             raise ValueError("N-gram embedding requires context length >= 1.")
diff --git a/vllm/v1/worker/gpu_model_runner.py b/vllm/v1/worker/gpu_model_runner.py
index 46d882c..f87058c 100644
--- a/vllm/v1/worker/gpu_model_runner.py
+++ b/vllm/v1/worker/gpu_model_runner.py
@@ -589,9 +589,11 @@ class GPUModelRunner(
         # Only relevant for models using ALiBi (e.g, MPT)
         self.use_alibi = model_config.uses_alibi
 
-        # PLE
+        # PLE is deliberately confined to the first PP stage: the checkpoint's
+        # PLE lives at layer 1 (zero-based), and only that stage receives raw
+        # input_ids. Other PP stages must not allocate or inject PLE metadata.
         ple_layer_ids = getattr(model_config.hf_text_config, "ple_layer_ids", ())
-        self.uses_ngram_embedding = bool(ple_layer_ids)
+        self.uses_ngram_embedding = bool(ple_layer_ids) and get_pp_group().is_first_rank
         if self.uses_ngram_embedding:
             self.ngram_context_len = int(model_config.hf_text_config.ngram_size) - 1
             self.ngram_eos_token_id = int(model_config.hf_text_config.eos_token_id)
@@ -600,13 +602,6 @@ class GPUModelRunner(
             self.ngram_eos_token_id = 0
         if self.uses_ngram_embedding and self.ngram_context_len <= 0:
             raise ValueError("N-gram embedding requires context length >= 1.")
-        if self.uses_ngram_embedding and len(get_pp_group().ranks) > 1:
-            raise RuntimeError(
-                "N-gram PLE embedding currently requires "
-                "pipeline_parallel_size=1. With PP>1, ngram context is only "
-                "injected on the first rank and can become incorrect on "
-                "non-first ranks. Please run with PP=1."
-            )
 
         self.cascade_attn_enabled = not self.model_config.disable_cascade_attn
         self.is_mm_prefix_lm = self.model_config.is_mm_prefix_lm
diff --git a/vllm/v1/worker/gpu_worker.py b/vllm/v1/worker/gpu_worker.py
index 69dd3de..19170c1 100644
--- a/vllm/v1/worker/gpu_worker.py
+++ b/vllm/v1/worker/gpu_worker.py
@@ -203,9 +203,24 @@ class Worker(WorkerBase):
         self._sleep_mode_backend: SleepModeBackend | None = None
 
     def _has_ple_layers(self) -> bool:
-        """Return whether this model configuration constructs PLE layers."""
+        """Return whether this worker owns PLE layers.
+
+        Qwen3.8 PLE is required to be in the first PP stage. Restricting the
+        offload client to that stage avoids empty registrations from the other
+        stages and preserves the existing DP/TP fan-out inside the stage.
+        """
         if not envs.VLLM_PLE_CPU_OFFLOAD:
             return False
+        nvme_path = os.getenv("VLLM_PLE_NVME_PATH", "").strip()
+        if nvme_path:
+            # The initial NVMe implementation is deliberately TP1/DP1. In that
+            # layout global rank zero is also the first PP stage; avoid reading
+            # get_pp_group() here because Worker construction can precede model
+            # parallel process-group initialization.
+            if self.parallel_config.tensor_parallel_size != 1 or self.rank != 0:
+                return False
+        elif self.parallel_config.pipeline_parallel_size != 1:
+            return False
         text_config = self.model_config.hf_text_config
         return bool(getattr(text_config, "ple_layer_ids", None))
 
@@ -226,7 +241,16 @@ class Worker(WorkerBase):
                 f"({parallel_config.data_parallel_size_local}/"
                 f"{parallel_config.data_parallel_size} local ranks)"
             )
-        if parallel_config.pipeline_parallel_size != 1:
+        if os.getenv("VLLM_PLE_NVME_PATH", "").strip():
+            if parallel_config.tensor_parallel_size != 1:
+                unsupported.append(
+                    f"NVMe PLE requires TP=1 (got TP={parallel_config.tensor_parallel_size})"
+                )
+            if parallel_config.data_parallel_size != 1:
+                unsupported.append(
+                    f"NVMe PLE requires DP=1 (got DP={parallel_config.data_parallel_size})"
+                )
+        elif parallel_config.pipeline_parallel_size != 1:
             unsupported.append(f"PP={parallel_config.pipeline_parallel_size}")
         if parallel_config.prefill_context_parallel_size != 1:
             unsupported.append(f"PCP={parallel_config.prefill_context_parallel_size}")
